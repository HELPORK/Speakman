# Only needed in production (local dev uses the Vite proxy in vite.config.js instead).
# Set this on your hosting platform (e.g. Render's Environment tab for the frontend
# static site) to your deployed backend's URL, including the /api suffix:
VITE_API_URL=https://your-backend-service.onrender.com/api
